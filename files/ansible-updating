For the mac:
  sudo pip install --upgrade ansible
  
Ubuntu:
  apt-get install ansible --upgrade
  # see http://docs.ansible.com/ansible/latest/installation_guide/intro_installation.html#latest-releases-via-apt-ubuntu
